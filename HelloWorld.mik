/*
	- mikf:
		is the keyword used for funtions
	
	- __noret__:
		tells the compiler that returning to a value is optional
	
	-- puts(str string) -> int:
		declares a function "puts" that takes in a string as argument and returns an integer.
	
	-- missing curly braces:
		inidcates a declaration rather than a definition
*/
mikf __noret__ puts(str string) -> int

str my_string = "Hello, World"
printf(my_string)

